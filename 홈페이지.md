# 홈페이지.md — 도구(Claude Code)가 읽는 절차서

이 파일은 사람이 아니라 **도구가 읽는** 절차서다. 사용자가 「홈페이지.md 의 N번 해 줘」라고 하면
그 번호의 절차를 순서대로 실행한다. 사용자는 비개발자다.

## 공통 규칙
- 열쇠 값(.env 안의 내용)은 절대 화면에 보여 주지 않고, 어떤 파일에도 옮겨 적지 않는다.
- 보고는 짧은 한국어로 한다. 명령어 원문은 사용자가 묻지 않는 한 보여 주지 않는다.
- 설치·로그인처럼 사용자가 창에서 직접 눌러야 하는 순간이 오면, 그 시점에 무엇을 누르면 되는지 한 줄로 알려 준다.
- 실패하면 원인을 사용자의 말로 한 줄, 다음에 할 일을 한 줄로 알려 준다.

## 1번 · 준비물 설치 (git, gh)
1. `git --version` 과 `gh --version` 으로 둘 다 있는지 확인한다.
2. 없는 것만 설치한다.
   - `winget install --id Git.Git -e --source winget --accept-package-agreements --accept-source-agreements`
   - `winget install --id GitHub.cli -e --source winget --accept-package-agreements --accept-source-agreements`
   - 설치 중 Windows 「허용하시겠어요?」 창이 뜨면 사용자에게 「예를 눌러 주세요」라고 알린다.
3. 하나라도 새로 설치했다면 사용자에게 알린다: 「설치했어요. 앱을 완전히 닫았다가 다시 열고, 다시 1번을 시켜 주세요.」
   (새로 설치된 프로그램은 앱을 다시 열어야 인식된다.)
4. 둘 다 있으면 「준비 완료 · git x.x · gh x.x」로 보고한다.
5. `winget` 자체가 없으면 「Windows가 오래돼 자동 설치가 안 돼요. git-scm.com 과 cli.github.com 에서 받아 설치한 뒤 다시 1번을 시켜 주세요」라고 안내한다.

## 2번 · GitHub 로그인
1. `gh auth status` 로 이미 로그인돼 있는지 본다. 돼 있으면 계정 이름만 보고하고 끝낸다.
2. 안 돼 있으면 `gh auth login --hostname github.com --git-protocol https --web` 를 실행한다.
   출력에 나오는 8자리 코드(XXXX-XXXX)와 주소 https://github.com/login/device 를 **즉시** 사용자에게 알린다.
   「브라우저에서 이 주소를 열고 코드를 넣은 뒤 Authorize 를 눌러 주세요. 지금 기다리고 있어요.」
   가능하면 `start https://github.com/login/device` 로 브라우저를 열어 준다.
3. 로그인이 끝나면 `gh auth setup-git` 을 실행한다(git 이 gh 의 로그인을 같이 쓰게).
4. `gh auth status` 로 계정 이름을 확인해 「〈아이디〉로 로그인됐어요」라고 보고한다.
5. 시간 초과나 취소로 실패하면: 「코드를 넣기 전에 시간이 지났어요. 다시 2번을 시켜 주시고, 코드가 나오면 바로 넣어 주세요.」

## 3번 · 올리기 (저장소 만들기 + 파일 올리기)
1. 이 폴더에 `.gitignore` 를 만든다. 내용: `.env` · `*.jpg` · `*.jpeg` · `*.png` · `*.webp` · `token.txt`.
   (열쇠와 참고 그림은 올리지 않는다. 그림은 남의 저작물이다.)
2. `dashboard.html` 을 `index.html` 로 복사한다(이미 있으면 덮어쓴다).
3. 아직 git 저장소가 아니면 `git init -b main`. `git config user.name` 이 비어 있으면 gh 계정 이름으로,
   `user.email` 이 비어 있으면 `〈아이디〉@users.noreply.github.com` 으로 이 저장소에만 설정한다.
4. `git add -A` → `git commit -m "morningbox: 첫 올리기"`.
5. **여기서 한 번 멈춘다(반드시).** 공개 저장소를 만들기 전에 사용자에게 「공개되는 파일」 목록을 보여 주고,
   「공개로 올려도 돼」라는 답을 받은 뒤에만 다음으로 간다. 목록엔 .env 와 그림 파일이 없어야 하며, 있으면 스스로 빼고 다시 보여 준다.
   (인터넷에 공개되는 일은 사람이 확인한다 — 이 멈춤은 절차의 일부다.)
6. `gh repo create morningbox --public --source . --remote origin --push`
   이름이 이미 있다는 오류면 `morningbox2` 로 다시 시도하고 사용자에게 이름이 바뀌었다고 알린다.
7. 올린 뒤 확인: `gh api repos/{owner}/{repo}/contents --jq ".[].name"` 결과에 `.env` 가 **없어야** 한다.
   있으면 즉시 사용자에게 경고하고, `git rm --cached .env` → 커밋 → 푸시로 지운 뒤
   「텔레그램 BotFather 에서 /revoke 로 열쇠를 새로 받아 .env 를 고쳐 주세요. 한 번 올라간 열쇠는 지워도 안전하지 않아요」라고 안내한다.
8. 「https://github.com/〈아이디〉/morningbox 에 올렸어요 · .env 없음 확인」으로 보고한다.

## 4번 · 홈페이지 켜기 (GitHub Pages)
1. `gh api -X POST repos/{owner}/{repo}/pages -f build_type=legacy -f "source[branch]=main" -f "source[path]=/"`
   「이미 있다」는 오류(409)면 그냥 넘어간다.
2. `gh api repos/{owner}/{repo}/pages --jq .html_url` 로 홈페이지 주소를 얻는다.
3. 60~120초 기다린 뒤 그 주소에 접속해 본다. 열리면 「열림 확인」, 아직 404면 「아직 만드는 중이에요. 2~3분 뒤 휴대폰에서 열어 보세요」.
4. 주소를 사용자에게 보고하고 `start 〈주소〉` 로 브라우저에 열어 준다.
5. 「업그레이드가 필요하다」는 류의 오류면 저장소가 비공개인 것이다.
   `gh repo edit --visibility public --accept-visibility-change-consequences` 뒤 1부터 다시 한다.

## 5번 · 스킬에 「올리기」 붙이기
0. `.claude/skills/morning-board/SKILL.md` 가 **없으면** 먼저 만든다(사용자가 8단계를 건너뛴 경우). 내용 = 이 폴더에서 한 순서 그대로:
   ① 뉴스(news.md)·날씨(weather.md · weather_alert.md) 가져오기 ② Design.md 기준으로 dashboard.html 만들기. 열쇠 값은 적지 않고 .env 를 읽으라고만 적는다.
   만들었다면 사용자에게 「스킬이 없어서 새로 만들었어요」라고 알린다.
1. `.claude/skills/morning-board/SKILL.md` 의 절차 끝에 두 단계를 추가한다.
   - dashboard.html 을 index.html 로 복사한다.
   - `git add -A` → `git commit -m "갱신"` → `git push` (바뀐 것이 없으면 그냥 넘어간다).
2. SKILL.md 어디에도 열쇠 값이 없는지 확인한다.
3. 「이제 /morning-board 를 실행하면 홈페이지도 같이 바뀌어요. PC와 앱이 켜져 있을 때만요」라고 보고한다.
